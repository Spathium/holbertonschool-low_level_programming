#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int ent;

	for (ent = 0; ent <= 9; ent++)
	printf("%d", ent);
	printf("\n");

	return (0);
}
